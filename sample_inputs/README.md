# Sample inputs

Large generated catalogs are not tracked in this repository.

To create a catalog locally, use:

python scripts/generate_catalog.py --n_sims 100 --out_path catalog/sim_catalog.csv --lon_deg 93.62

Users can then run the geometry and Helios pipeline from that catalog.
